spring.application.name=SegundaVida

# Puerto de la aplicación (Render define PORT automáticamente)
server.port=${PORT:8080}

# Configuración de la base de datos PostgreSQL (Render)
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mostrar SQL en consola
spring.jpa.show-sql=true

# (Opcional) Si no usas Redis aún, comenta esta parte
# spring.session.store-type=redis
# spring.redis.host=localhost
# spring.redis.port=6379

# H2 ya no se usa, así que se comenta
# spring.h2.console.enabled=true
